{
  "name": "job-hunt-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:browser": "node scripts/build-browser-bundle.mjs",
    "check": "node scripts/build-browser-bundle.mjs && node --check dist/app.bundle.js && node --test tests/*.test.js",
    "test": "node --test tests/*.test.js"
  }
}
